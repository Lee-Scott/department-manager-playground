package com.familyfirstsoftware.departmentManager;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/*
	to run on command line use: mvn spring-boot: run
	from the directory that you want to run
	use ctrl + c to stop it
 */
@SpringBootApplication
public class DemoTwoApplication {

	public static void main(String[] args) {
		SpringApplication.run(DemoTwoApplication.class, args);
	}

}
